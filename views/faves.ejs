
    <h1>Faves Page</h1>
<ul>
    <% favorites.forEach((book)=>{ %>
        <li>
            <h3><%= book.title %></h3>
            <a href="/results/<%= book.bookId %>">Details</a>
        </li>
    <% }) %> 
</ul>